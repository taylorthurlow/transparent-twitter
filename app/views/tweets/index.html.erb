<div class="container">
  <h1>Tweets</h1>

  <% if flash[:notice] %>
    <p id="notice"><%= flash[:notice] %></p>
  <% end %>

  <%= render partial: 'tweets/table', locals: { tweets: @tweets } %>
</div>